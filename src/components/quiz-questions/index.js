import React from "react";
import Question from "../question"
const QuizQuestions = ({ questions, answer }) => {
  console.log(questions, answer);
  return <Question></Question>
};

export default QuizQuestions;
